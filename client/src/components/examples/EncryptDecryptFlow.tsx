import { EncryptDecryptFlow } from "../EncryptDecryptFlow";

export default function EncryptDecryptFlowExample() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 p-8">
      <div className="mx-auto max-w-7xl">
        <EncryptDecryptFlow />
      </div>
    </div>
  );
}
